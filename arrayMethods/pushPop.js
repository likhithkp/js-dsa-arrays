// Push and Pop is used to add or remove the element at the end of any array
let num = [1, 2, 3, 4, 5];

// Add/Push
num.push(10); // [1, 2, 3, 4, 5, 10]

// Remove/Pop
num.pop(); // [1, 2, 3, 4, 5], since 10 is added above or else [1, 2, 3, 4]

// Push/Pop are more performant than Shift/Unshift